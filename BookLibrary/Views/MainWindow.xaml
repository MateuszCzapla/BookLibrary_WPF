<Window x:Class="BookLibrary.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BookLibrary.Views"
        xmlns:vm="clr-namespace:BookLibrary.ViewModels"
        mc:Ignorable="d"
        Title="Book Library" Height="450" Width="800">
    
    <Window.Resources>
        <vm:LibraryGridViewModel x:Key="libraryGridViewModel" />
    </Window.Resources>

    <Grid DataContext="{StaticResource libraryGridViewModel}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>

        <Menu x:Name="mainMenu" Grid.Row="0" Grid.ColumnSpan="2">
            <MenuItem Header="_File">
                <MenuItem Command="ApplicationCommands.Close"/>
            </MenuItem>
            <MenuItem Header="_Edit">
                <MenuItem Command="ApplicationCommands.Copy"/>
                <MenuItem Command="ApplicationCommands.Cut"/>
                <MenuItem Command="ApplicationCommands.Paste"/>
            </MenuItem>
            <MenuItem Header="_Help" />
        </Menu>

        <GroupBox Header="Result" Margin="10" Padding="10" Grid.Column="0" Grid.Row="1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition />
                    <ColumnDefinition />
                    <ColumnDefinition />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="auto" />
                    <RowDefinition Height="auto" />
                </Grid.RowDefinitions>

                <DataGrid AlternatingRowBackground="Gray" VerticalAlignment="Stretch" Grid.Row="0" Grid.ColumnSpan="5" ItemsSource="{Binding Path=LibraryGrid}" />
                <Button Content="First" Grid.Column="0" Grid.Row="1" Command="{Binding FirstPageCommand}" CommandParameter="{StaticResource libraryGridViewModel}" />
                <Button Content="Previous" Grid.Column="1" Grid.Row="1" Command="{Binding PreviousPageCommand}" CommandParameter="{StaticResource libraryGridViewModel}" />
                <Label Grid.Column="2" Grid.Row="1" Content="{Binding PageDisplay}" />
                <Button Content="Next" Grid.Column="3" Grid.Row="1" Command="{Binding NextPageCommand}" CommandParameter="{StaticResource libraryGridViewModel}" />
                <Button Content="Last" Grid.Column="4" Grid.Row="1" Command="{Binding LastPageCommand}" CommandParameter="{StaticResource libraryGridViewModel}" />
                
                <TextBox Grid.Column="0" Grid.Row="2" Text="{Binding Path=RowsCount}"/>
            </Grid>
        </GroupBox>

        

        <GroupBox Header="Query" Margin="10" Padding="10" Grid.Column="1" Grid.Row="1">
            <CheckBox Content="CheckBox" IsChecked="{Binding Path=EditMode}" />
        </GroupBox>
        <StatusBar Grid.ColumnSpan="2" Grid.Row="2">
            <StatusBarItem>
                <Label Content="test" />
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>
