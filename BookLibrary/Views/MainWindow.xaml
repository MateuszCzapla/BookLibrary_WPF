<Window x:Class="BookLibrary.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BookLibrary.Views"
        xmlns:vm="clr-namespace:BookLibrary.ViewModels"
        mc:Ignorable="d"
        Title="Book Library" Height="450" Width="800">
    
    <Window.Resources>
        <vm:LibraryGridViewModel x:Key="libraryGridViewModel" />
    </Window.Resources>

    <Grid DataContext="{StaticResource libraryGridViewModel}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
            <ColumnDefinition />
            <ColumnDefinition />
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>

        <Menu x:Name="mainMenu" Grid.Row="0" Grid.ColumnSpan="6">
            <MenuItem Header="_File">
                <MenuItem Command="ApplicationCommands.Close"/>
            </MenuItem>
            <MenuItem Header="_Edit">
                <MenuItem Command="ApplicationCommands.Copy"/>
                <MenuItem Command="ApplicationCommands.Cut"/>
                <MenuItem Command="ApplicationCommands.Paste"/>
            </MenuItem>
            <MenuItem Header="_Help" />
        </Menu>

        <DataGrid
            x:Name="LibararyGrid"
            AlternatingRowBackground="Gray"
            Grid.Column="0"
            Grid.Row="1"
            Grid.ColumnSpan="5"
            VerticalAlignment="Stretch"
            ItemsSource="{Binding Path=LibraryGrid}"
        />
        <CheckBox Content="CheckBox" Grid.Column="5" Grid.Row="1" IsChecked="{Binding Path=EditMode}" />
        
        <Button Content="First" Grid.Column="0" Grid.Row="2" Command="{Binding FirstPageCommand}" CommandParameter="{StaticResource libraryGridViewModel}" />
        <Button Content="Previous" Grid.Column="1" Grid.Row="2" Command="{Binding PreviousPageCommand}" CommandParameter="{StaticResource libraryGridViewModel}" />
        <Label Grid.Column="2" Grid.Row="2" Content="{Binding PageDisplay}" />
        <Button Content="Next" Grid.Column="3" Grid.Row="2" Command="{Binding NextPageCommand}" CommandParameter="{StaticResource libraryGridViewModel}" />
        <Button Content="Last" Grid.Column="4" Grid.Row="2" Command="{Binding LastPageCommand}" CommandParameter="{StaticResource libraryGridViewModel}" />
        <TextBox Grid.Column="5" Grid.Row="2" Text="{Binding Path=RowsCount}"/>

        <StatusBar Grid.ColumnSpan="6" Grid.Row="3">
            <StatusBarItem>
                <Label Content="test" />
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>
