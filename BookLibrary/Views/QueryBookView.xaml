<UserControl x:Class="BookLibrary.Views.QueryBookView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewmodels="clr-namespace:BookLibrary.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>
        <viewmodels:QueryBookViewModel x:Key="queryBookViewModel" />
    </UserControl.Resources>

    <Grid DataContext="{StaticResource queryBookViewModel}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>

        <Label Grid.Row="0" Grid.Column="0" Content="Title" />
        <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding Title}" />
        <Label Grid.Row="1" Grid.Column="0" Content="Year" />
        <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding Year}" PreviewTextInput="NumberValidation" />
        <Label Grid.Row="2" Grid.Column="0" Content="Modify date (from)" />
        <Label Grid.Row="3" Grid.Column="0" Content="{Binding Date_from}" />
        <Calendar Grid.Row="3" Grid.Column="1" SelectedDate="{Binding Date_from}"/>
        <Label Grid.Row="4" Grid.Column="0" Content="Modify date (to)" />
        <Label Grid.Row="5" Grid.Column="0" Content="{Binding Date_to}" />
        <Calendar Grid.Row="5" Grid.Column="1" SelectedDate="{Binding Date_to}" IsEnabled="False" />
        <Button Grid.Row="6" Grid.Column="0" Grid.ColumnSpan="2" Content="Search" Command="{Binding SearchCommand}" CommandParameter="{StaticResource queryBookViewModel}" />
    </Grid>
</UserControl>
